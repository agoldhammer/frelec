# General instructions

## The remote server is con1. 

## When asked to check recency of deployment: Examine each .png file in this directory, then check that there is a .png file with the same name in /var/www/html/polls on the remote server, and that the date of the remote file is later than the local one. If any of the remote files is older, push the corresponding newer local file up to the polls directory on the server.

## When asked to check for new polls, see if there has been any update on the original source from which the polling data was download.

